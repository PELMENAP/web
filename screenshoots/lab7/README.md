# 🚀 Лабораторная работа №7: Очереди сообщений

## 📁 Структура проекта

```
/lab7
 ├─ docker-compose.yml
 ├─ Dockerfile
 ├─ composer.json
 ├─ nginx.conf
 ├─ www/
 │   ├─ index.php          (веб-интерфейс со статистикой)
 │   ├─ send.php           (отправка в очередь)
 │   ├─ worker.php         (обработчик сообщений)
 │   ├─ RabbitManager.php  (менеджер RabbitMQ)
 │   └─ KafkaManager.php   (менеджер Kafka)
```

---

## ⚙️ Запуск проекта

### 1. Создайте папку `www` и скопируйте туда все PHP файлы

```bash
mkdir www
```

### 2. Запустите контейнеры

```bash
docker-compose up -d --build
```

### 3. Дождитесь запуска всех сервисов (30-60 секунд)

```bash
docker-compose logs -f
```

---

## 🎯 Использование

### Веб-интерфейс
Откройте: **http://localhost:8080**

### RabbitMQ Management Panel
Откройте: **http://localhost:15672**
- Login: `guest`
- Password: `guest`

---

## 🔧 Запуск Worker (обработчика)

### Для RabbitMQ:
```bash
docker exec -it lab7_php php worker.php rabbitmq
```

### Для Kafka:
```bash
docker exec -it lab7_php php worker.php kafka
```

Worker будет обрабатывать сообщения и автоматически отправлять ошибочные во вторую очередь.

---

## 📊 Логика обработки ошибок

1. **Отправка**: Сообщение попадает в основную очередь
2. **Обработка**: Worker получает сообщение
3. **Проверка**: Если `random > 70` — это ошибка!
4. **Ошибка**: Сообщение автоматически отправляется в `error_queue`
5. **Успех**: Сообщение сохраняется в лог-файл

---

## 🧪 Тестирование

1. Откройте http://localhost:8080
2. Отправьте несколько сообщений (5-10 штук)
3. Запустите worker в отдельном терминале
4. Обновите страницу — увидите обновлённую статистику
5. Проверьте RabbitMQ Management — там будут видны обе очереди

---

## 📝 Файлы логов

Worker сохраняет успешно обработанные сообщения в:
```
www/processed_2024-12-10.log
```

---

## 🛑 Остановка проекта

```bash
docker-compose down
```

Для полной очистки (включая volumes):
```bash
docker-compose down -v
```

---

## 🎓 Что реализовано

✅ RabbitMQ и Kafka в одном docker-compose  
✅ Две очереди/топика (основная + ошибки)  
✅ Worker с обработкой ошибок  
✅ Автоматическая отправка ошибок во вторую очередь  
✅ Статистика на веб-странице  
✅ Красивый интерфейс с real-time обновлением